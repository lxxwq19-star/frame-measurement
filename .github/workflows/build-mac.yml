name: Build Mac Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-mac:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build Mac app
        run: npm run build -- --mac
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Upload DMG to Release
        run: |
          gh auth setup-git --hostname github.com --git-protocol https || true
          gh release create v1.4.0 dist/*.dmg --title "Mac v1.4.0" --generate-notes --clobber || true
        env:
          GH_TOKEN: ${{ github.token }}

